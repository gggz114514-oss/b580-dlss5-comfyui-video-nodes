# 阶段验证记录

日期：2026-09-11。以下是开发者本机完成的内部实验，不是所有设备兼容性保证。

| 项目 | 已观察结果 |
|---|---|
| 单 NR 快速版已安装节点 | 243帧，864×480，24fps，音频保留 |
| 单 NR 精确版真实 ComfyUI LoadVideo→NR→SaveVideo | 执行成功；NR成片和SaveVideo成片均独立CPU解码243帧，864×480，24fps，含音轨 |
| 精确版与快速版内部SR/FG链路 | 1×AA+FG及2×SR+FG曾完成243源帧→485输出帧测试 |
| 真实精确节点GPU链路 | Block242次；单NR时SR/FG/depth为0，记录的CPU像素传输为0 |
| 运行环境隔离 | target与冻结精确缓存相同，5项SYCL/UR DLL来自选定Python运行库目录 |

真实精确节点 SaveVideo 成片 SHA-256（仅提供摘要，不上传用户视频）：

`35799a161cd8d63442b3ba46dcba3f0227f5eada5f6472734fe17ba3a1462846`

技术解码通过不等于画质等价。用户反馈快速版画面差异明显，因此节点明确命名“快速版（类 DLSS5）”。精确节点回归没有重新进行4060逐字节对照。

上述记录来自本机已运行版本。公开节点适配层将固定路径改为配置项；首次发布只对其导入、节点注册、配置校验完成CPU检查。完整后端未随包分发，尚无干净机器安装验收。

## 便携候选包追加验证（2026-09-11）

使用独立复制的 Python、Torch、Triton、预编译缓存和原生运行库，通过公开节点调用：

| 路线 | 独立解码结果 | 缓存及GPU链路 |
|---|---|---|
| 精确单NR | 243帧、864×480、24fps、音轨完整 | 无现场编译；Block242，记录的CPU像素传输为零 |
| 快速NR＋2×SR＋FG | 485帧、1728×960、48fps、音轨完整 | 无现场编译；原始Block242由SR/FG复用，记录的CPU像素传输为零 |

两轮均已完成监控报告核对。

最终分发验证还完成了：中文、空格、emoji 的原生文件I/O探针；从三份ZIP安装到独立中文路径、在线获取并校验模型、默认配置发现；精确1×AA+FG独立解码485帧/48fps，快速单NR独立解码243帧/24fps，均为864×480并保留音轨。两例worker返回0，记录的Block为242，CPU像素传输为0，没有运行时编译。原生路径修复前后的颜色着色器字节相同。

这些是同一台B580机器上的独立目录验证。安装测试使用本地Release附件和在线模型获取；不等于任意显卡、驱动或其他机器均已测试。

分片传输版补充验证：从29个本地分片在全新中文路径安装，29,066个文件逐一SHA校验与已验收包相符，模型SHA与最终安装根标记通过。分片不更改运行二进制，因此本轮没有重复GPU测试。
